local prefix = "September.pkg."

local key_one = require(prefix .. "key_one")
local key_kanon = require(prefix .. "key_kanon")
local key_air = require(prefix .. "key_air")
local key_clannad = require(prefix .. "key_clannad")
local key_littlebusters = require(prefix .. "key_littlebusters")
local key_rewrite = require(prefix .. "key_rewrite")
local key_angelbeats = require(prefix .. "key_angelbeats")
local key_charlotte = require(prefix .. "key_charlotte")
local key_harmonia = require(prefix .. "key_harmonia")
local key_summerpockets = require(prefix .. "key_summerpockets")
local key_kamisamaninattahi = require(prefix .. "key_kamisamaninattahi")
local key_loopers = require(prefix .. "key_loopers")
local key_lunaria = require(prefix .. "key_lunaria")
local key_heavenburnsred = require(prefix .. "key_heavenburnsred")

Fk:loadTranslationTable({
	["September"] = "九月"
})

return {
	key_one,
	key_kanon,
	key_air,
	key_clannad,
	key_littlebusters,
	key_rewrite,
	key_angelbeats,
	key_charlotte,
	key_harmonia,
	key_summerpockets,
	key_kamisamaninattahi,
	key_loopers,
	key_lunaria,
	key_heavenburnsred,
}